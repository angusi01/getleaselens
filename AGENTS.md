# GetLeaseLens release rule

This app is the independent GitHub repository `angusi01/getleaselens` and Vercel project `prj_ZU9ye5batv8k0gM3DNM5cOhtJmQW`.

Push its complete intended source state to the repository's `main` branch, deploy only to that Vercel project, and verify `getleaselens.com.au` plus `www.getleaselens.com.au`. A `passive-portfolio` monorepo push is not a substitute for this app's repository push and deployment.
